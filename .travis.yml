language: node_js
node_js:
  - "6"
  - "4"
notifications:
  irc:
    channels:
      - "chat.freenode.net#wikimedia-editing"
    template:
      - "%{repository}#%{build_number} (%{branch} - %{commit} %{author}): %{message} - %{build_url}"
